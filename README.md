# Restaurant Search Backend

Node.js + MySQL backend for the assignment.

## Run locally
1. Install dependencies: cd backend; npm ci
2. Create .env with DB credentials (or use docker-compose example below)
3. Start: npm start

## Docker
docker compose up --build
