{
  "name": "restaurant-search-backend",
  "version": "1.0.0",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test:newman": "newman run postman/Restaurant_Search_API_Final.postman_collection.json -e postman/restaurant_api_environment.postman_environment.json --reporters cli,junit --reporter-junit-export ./reports/junit.xml --bail"
  },
  "dependencies": {
    "express": "^4.18.2",
    "mysql2": "^3.6.0",
    "dotenv": "^16.0.0"
  },
  "devDependencies": {
    "newman": "^5.3.0"
  }
}
